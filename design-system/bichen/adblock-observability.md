# 去广告可观测性

- 应用保护必须直接显示实时 DNS 请求、累计拦截、本次请求、本次拦截、拦截率与最近拦截域名。
- 运行中的统计读取内存计数器，不依赖 10 秒持久化周期。
- 请求记录默认保持隐私友好关闭；用户可在活动页开启，最多保留最近 100 条。
- 每条记录必须正确区分 blocked/allowed，并使用可读时间。
- 模块 hosts 模式不伪造逐次命中数；显示模块状态、有效规则、拦截规则、放行规则，并明确说明统计能力差异。
- 首页必须让用户一眼确认“是否正在保护”和“是否真的发生过拦截”。
